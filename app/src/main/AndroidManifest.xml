<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.capstone_employee">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Capstone_employee"
        android:usesCleartextTraffic="true">
        <activity android:name=".attendance.AttendanceActivity"></activity>
        <activity android:name=".salary.ViewPayslip" />
        <activity android:name=".mail.ViewMail" />
        <activity android:name=".leave.LeaveStatus" />
        <activity android:name=".ui.home.TimeReport" />
        <activity android:name=".mail.MailStatus" />
        <activity android:name=".ui.profile.AuthenticateActivity" />
        <activity android:name=".ui.profile.AccountActivity" />
        <activity android:name=".ui.profile.EditActivity" />
        <activity
            android:name=".recruit.RecruitActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".salary.SalaryActivity" />
        <activity android:name=".mail.MailActivity" />
        <activity android:name=".leave.LeaveActivity" />
        <activity android:name=".ResignActivity" />
        <activity android:name=".BottomNav">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Forgot_password" />
        <activity android:name=".Splashscreen" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>